{%- for block in section.blocks -%}
	{%- if block.settings.text != blank -%}
		{%- case block.type -%}
			{%- when 'announcement' -%}
				<div
					class="text-white bg-gray-very-dark py-4 text-xs text-center {{ block.settings.text_alignment }}"
					role="region" aria-label="{{ 'sections.header.announcement' | t }}"
					{{ block.shopify_attributes }}
				>
					<div class="container">
						<div class="row">
							<div>
								{%- if block.settings.link != blank -%}<a class="hover:underline" href="{{ block.settings.link }}" class="">{%- endif -%}

								<p class="h5">
									{{ block.settings.text | escape }}
								</p>

								{%- if block.settings.link != blank -%}</a>{%- endif -%}
							</div>
						</div>
					</div>
				</div>
		{%- endcase -%}
	{%- endif -%}
{%- endfor -%}

{% schema %}
{
  "name": "t:sections.announcement-bar.name",
  "max_blocks": 12,
  "blocks": [
    {
      "type": "announcement",
      "name": "t:sections.announcement-bar.blocks.announcement.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "default": "Welcome to our store",
          "label": "t:sections.announcement-bar.blocks.announcement.settings.text.label"
        },
        {
          "type": "url",
          "id": "link",
          "label": "t:sections.announcement-bar.blocks.announcement.settings.link.label"
        }
      ]
    }
  ],
  "default": {
    "blocks": [
      {
        "type": "announcement"
      }
    ]
  }
}
{% endschema %}
