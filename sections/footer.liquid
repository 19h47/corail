{% comment %}theme-check-disable UndefinedObject{% endcomment %}
{{ 'section-footer.css' | asset_url | stylesheet_tag }}

<link rel="stylesheet" href="{{ 'component-newsletter.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-list-payment.css' | asset_url }}" media="print" onload="this.media='all'">

<noscript>{{ 'component-newsletter.css' | asset_url | stylesheet_tag }}</noscript>
<noscript>{{ 'component-list-payment.css' | asset_url | stylesheet_tag }}</noscript>

<footer class="mb-11 lg:mb-0 border-orange-grayish border-b-[0.0625rem] lg:border-none relative">
	<div class="relative lg:h-[22.75rem]">

		<div class="flex flex-col lg:flex-row lg:h-full">

			<a href="{{ routes.root_url }}" class="after:pb-[100%] w-[22.75rem] after:block relative hidden lg:block">
				{% render 'svg', icon: 'c-logo', classes: 'absolute inset-0 m-auto h-10 w-10 fill-current', description: shop.name %}
			</a>

			<hr class="w-full h-[0.0625rem] lg:h-full block lg:w-[0.0625rem] bg-orange-grayish"></hr>

			<div class="flex flex-1 py-24 lg:py-0 justify-center items-center font-semibold font-display text-xl leading-5 text-center">
				<p>
					FROM THE SEA<br>
					TO SAVE THE EARTH
				</p>
			</div>

			<span class="h-[0.0625rem] lg:h-full block w-full lg:w-[0.0625rem] bg-orange-grayish"></span>

			<div class="text-orange-grayish px-5 lg:px-14 py-8 lg:pb-12 flex flex-col lg:items-end">

				<coordinates-dms class="font-bold lg:text-right leading-5 text-sm">
					<span class="block" style="font-variant-numeric: tabular-nums;">43°17′42,00″ N</span>
					<span class="block" style="font-variant-numeric: tabular-nums;">5°22′19,20″ E</span>
				</coordinates-dms>

				<nav class="text-orange-dark-grayish mt-14 lg:mt-auto">
					<ul role="list" class="inline-flex gap-8">
						{%- if settings.social_twitter_link != blank -%}
							<li>
								<a href="{{ settings.social_twitter_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'twitter', classes: 'fill-current w-full h-full', description: 't:general.social.links.twitter' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_facebook_link != blank -%}
							<li>
								<a href="{{ settings.social_facebook_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'facebook', classes: 'fill-current w-full h-full', description: 't:general.social.links.facebook' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_pinterest_link != blank -%}
							<li>
								<a href="{{ settings.social_pinterest_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'pinterest', classes: 'fill-current w-full h-full', description: 't:general.social.links.pinterest' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_instagram_link != blank -%}
							<li>
								<a href="{{ settings.social_instagram_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'instagram', classes: 'fill-current w-full h-full', description: 't:general.social.links.instagram' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_twitch_link != blank -%}
							<li>
								<a href="{{ settings.social_twitch_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'twitch', classes: 'fill-current w-full h-full', description: 't:general.socal.links.twitch' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_tiktok_link != blank -%}
							<li>
								<a href="{{ settings.social_tiktok_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'tiktok', classes: 'fill-current w-full h-full', description: 't:general.social.links.tiktok' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_tumblr_link != blank -%}
							<li>
								<a href="{{ settings.social_tumblr_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'tumblr', classes: 'fill-current w-full h-full', description: 't:general.social.links.tumblr' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_snapchat_link != blank -%}
							<li>
								<a href="{{ settings.social_snapchat_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'snapchat', classes: 'fill-current w-full h-full', description: 't:general.social.links.snapchat' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_youtube_link != blank -%}
							<li>
								<a href="{{ settings.social_youtube_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'youtube', classes: 'fill-current w-full h-full', description: 't:general.social.links.youtube' -%}
								</a>
							</li>
						{%- endif -%}

						{%- if settings.social_vimeo_link != blank -%}
							<li>
								<a href="{{ settings.social_vimeo_link }}" class="w-4 h-4 block">
									{%- render 'svg', icon: 'vimeo', classes: 'fill-current w-full h-full', description: 't:general.socal.links.vimeo' -%}
								</a>
							</li>
						{%- endif -%}
					</ul>
				</nav>
			</div>

		</div>

		<hr class="bg-orange-grayish absolute top-0 inset-x-0 w-full h-[0.0625rem]">
	</div>

	<div class="relative">
		<hr class="bg-orange-grayish absolute top-0 inset-x-0 w-full h-[0.0625rem]">
		<div class="py-11 lg:py-6 px-5 lg:px-4 text-display" {{ block.shopify_attributes }}>
			{%- for block in section.blocks -%}

				{%- if block.type == 'link_list' and block.settings.menu != blank -%}
					<nav class="text-orange-dark-grayish font-display">
						<ul class="inline-flex flex-col lg:flex-row gap-y-4 lg:gap-y-0 lg:gap-x-8">
							{%- for link in block.settings.menu.links -%}
								<li>
									<a class="hover:underline flex items-center flex-shrink-0 font-medium {% if link.active %} is-active {% endif %}" href="{{ link.url }}">
										{{- link.title -}}
										{%- render 'svg', icon: 'arrow-up-right', classes: 'ml-1.5 h-1.5 w-1.5 fill-current', description: link.title -%}
									</a>
								</li>
							{%- endfor -%}
						</ul>
					</nav>
				{%- endif -%}

			{%- endfor -%}
		</div>
	</div>

	<button type="button" class="flex gap-x-8 lg:gap-x-32 items-center group text-white bg-red-very-light px-5 py-1.5 lg:py-5 lg:pl-14 {% if request.page_type != 'product' %} fixed {% else %} absolute -mb-11 lg:mb-0 {% endif %} bottom-0 right-0 z-50">
		<p class="uppercase font-semibold font-display lg:text-xl leading-none lg:leading-6">{{ 'general.newsletter.label' | t }}</p>
		<p class="button-secondary inline-block align-middle group-hover:text-red-very-light group-hover:bg-white group-hover:border-red-very-light">
			<span class="hidden lg:inline">{{ 'general.newsletter.title' | t }}</span>
			<span class="text-xs lg:hidden">&rightarrow;</span>
		</p>
	</button>
</footer>



{% render 'icons' % %}

{% javascript %}
  class LocalizationForm extends HTMLElement {
    constructor() {
      super();
      this.elements = {
        input: this.querySelector('input[name="locale_code"], input[name="country_code"]'),
        button: this.querySelector('button'),
        panel: this.querySelector('.disclosure__list-wrapper')
      };
      this.elements.button.addEventListener('click', this.openSelector.bind(this));
      this.elements.button.addEventListener('focusout', this.closeSelector.bind(this));
      this.addEventListener('keyup', this.onContainerKeyUp.bind(this));

      this.querySelectorAll('a').forEach(item => item.addEventListener('click', this.onItemClick.bind(this)));
    }

    hidePanel() {
      this.elements.button.setAttribute('aria-expanded', 'false');
      this.elements.panel.setAttribute('hidden', true);
    }

    onContainerKeyUp(event) {
      if (event.code.toUpperCase() !== 'ESCAPE') {
		  return;
	  }

      this.hidePanel();
      this.elements.button.focus();
    }

    onItemClick(event) {
      event.preventDefault();
      const form = this.querySelector('form');
      this.elements.input.value = event.currentTarget.dataset.value;
      if (form) {
		  form.submit();
	  }
    }

    openSelector() {
      this.elements.button.focus();
      this.elements.panel.toggleAttribute('hidden');
      this.elements.button.setAttribute('aria-expanded', (this.elements.button.getAttribute('aria-expanded') === 'false').toString());
    }

    closeSelector(event) {
      const shouldClose = event.relatedTarget && event.relatedTarget.nodeName === 'BUTTON';
      if (event.relatedTarget === null || shouldClose) {
        this.hidePanel();
      }
    }
  }

  customElements.define('localization-form', LocalizationForm);
{% endjavascript %}

{% schema %}
{
  "name": "t:sections.footer.name",
  "blocks": [
    {
      "type": "link_list",
      "name": "t:sections.footer.blocks.link_list.name",
      "settings": [
        {
          "type": "link_list",
          "id": "menu",
          "default": "footer",
          "label": "t:sections.footer.blocks.link_list.settings.menu.label",
          "info": "t:sections.footer.blocks.link_list.settings.menu.info"
        }
      ]
    },
    {
      "type": "text",
      "name": "t:sections.footer.blocks.text.name",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "default": "Heading",
          "label": "t:sections.footer.blocks.text.settings.heading.label"
        }
      ]
    },
    {
      "type": "image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "range",
          "id": "image_width",
          "min": 50,
          "max": 200,
          "step": 5,
          "unit": "px",
          "label": "Image width",
          "default": 100
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__1.content",
      "info": "t:sections.footer.settings.header__1.info"
    },
    {
      "type": "checkbox",
      "id": "newsletter_enable",
      "default": true,
      "label": "t:sections.footer.settings.newsletter_enable.label"
    },
    {
      "type": "text",
      "id": "newsletter_heading",
      "default": "Subscribe to our emails",
      "label": "t:sections.footer.settings.newsletter_heading.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__2.content",
      "info": "t:sections.footer.settings.header__2.info"
    },
    {
      "type": "checkbox",
      "id": "show_social",
      "default": false,
      "label": "t:sections.footer.settings.show_social.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__3.content",
      "info": "t:sections.footer.settings.header__4.info"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.footer.settings.enable_country_selector.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__5.content",
      "info": "t:sections.footer.settings.header__6.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.footer.settings.enable_language_selector.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__7.content"
    },
    {
      "type": "checkbox",
      "id": "payment_enable",
      "default": true,
      "label": "t:sections.footer.settings.payment_enable.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    }
  ],
  "default": {
    "blocks": [
      {
        "type": "link_list"
      }
    ]
  }
}
{% endschema %}
