{% render 'annoucement', text: block.settings.text, link: block.settings.link, attributes: block.shopify_attributes %}

{% schema %}
{
  "name": "t:sections.announcement.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "default": "Welcome to our store",
      "label": "t:sections.announcement.settings.text.label"
    },
    {
      "type": "url",
      "id": "link",
      "label": "t:sections.announcement.settings.link.label"
    }
  ],
  "presets": [
    {
      "name": "t:sections.annoucement.presets.name"
    }
  ]
}
{% endschema %}
